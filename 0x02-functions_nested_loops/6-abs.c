/**
 * _abs - return the abs value
 *
 * @n : number
 *
 * Return: abs value
 */

int _abs(int n)
{
		if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (-1 * n);
	}

}
