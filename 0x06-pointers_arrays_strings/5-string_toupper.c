#include <string.h>

/**
 * string_toupper - enter
 * @c: c
 * Return: string
*/

char *string_toupper(char *c);
{
	int i;

	for (i = 0; i < strlen(c); i++)
	{
		if (a[i] >= 'a' && a[i] <= 'z')
			a[i] = (a[i] - 'a') + 'A'
	}
	return (c);
}
